export enum OrderType {
  delivery = "delivery",
  pickup = "pickup",
}

export enum OrderStatus {
  pending = "pending",
  processing = "processing",
  completed = "completed",
  cancelled = "cancelled",
}
